const path = require('path');

module.exports = {
  'config': path.resolve('./src/config', 'config.json'),// nói làm sao để kết nối đến db
  'migrations-path': path.resolve('./src', 'migrations'), //tạo các bảng trong db
  'models-path': path.resolve('./src', 'models'), // tạo 
  'seeders-path': path.resolve('./src', 'seeders') // tạo dl fake
}
